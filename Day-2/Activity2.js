//Assignment Operators

let variable = 12;

let number = 8;

//now add with assignment operator
variable += number;
console.log(variable);

//keep the value unchanged
variable=12;

// now substract using assignment operator
variable -= number;
console.log(variable);